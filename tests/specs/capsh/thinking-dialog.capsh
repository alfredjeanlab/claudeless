# Capture extended thinking toggle dialog.

wait "for shortcuts" 5s

# Open thinking dialog (Meta+T / Option+T)
send <M-t>
wait "thinking" 3s
snapshot "thinking_dialog"

# Close dialog
send <Esc> 100 <C-u> 100 "/exit" 100 <Enter>
wait 500
kill TERM
