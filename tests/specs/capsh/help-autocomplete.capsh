# Capture /help autocomplete suggestion (before pressing Enter).

wait "for shortcuts" 5s

send "/help"
wait "Show help" 3s
snapshot "help_autocomplete"

send <Esc> 100 <C-u> 100 "/exit" 100 <Enter>
wait 500
kill TERM
