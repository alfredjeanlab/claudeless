# Capture export method dialog.

wait "for shortcuts" 5s

# Type /export and wait for autocomplete, then submit
send "/export"
wait "/export" 3s
send <Enter>
wait "Select export method" 5s
snapshot "export_method_dialog"

# Exit (Esc to cancel dialog, then exit)
send <Esc> 100 <C-u> 100 "/exit" 100 <Enter>
wait 500
kill TERM
