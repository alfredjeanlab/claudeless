# Scenario: MCP Read File Test (using raw tool name)
# Uses raw MCP tool name for routing

name = "mcp-read-raw-test"

[tool_execution]
mode = "mock"

[[responses]]
pattern = { type = "contains", text = "read" }

[responses.response]
text = "I'll read the file for you."

[[responses.response.tool_calls]]
tool = "read_file"  # Raw MCP tool name (not qualified)
input = { path = "/tmp/mcp-test/sample.txt" }
result = "file contents from mock"
