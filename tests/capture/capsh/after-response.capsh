# Capture state after Claude responds to a simple prompt.
# Requires API call - sends a simple question to Haiku.
# Args: --model haiku
# Timeout: 60

wait "for shortcuts" 10s

# Send a simple prompt that won't trigger tool use
send "What is 2+2? Just give me the number."
wait 100
send <Enter>

# Wait for response to start (spinner appears, "for shortcuts" disappears)
wait !"for shortcuts" 10s

# Wait for response to complete (status bar returns to ready state)
wait "for shortcuts" 30s
snapshot "after_response"

# Exit cleanly
send <C-u> 100 "/exit" 100 <Enter>
wait 500
kill TERM
