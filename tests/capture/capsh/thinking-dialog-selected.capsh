# Capture thinking dialog with each option selected.
# Opens dialog mid-conversation and navigates to capture both states.
# Requires API call - Claude must respond to prompt.
# Args: --model haiku
# Timeout: 120

wait "─\n❯" 10s
wait 1s

# Build up conversation to make it mid-conversation
send "What is 2+2? Just give me the number." 1000 <Enter>
wait "esc to interrupt" 30s
wait !"esc to interrupt" 30s

# Open thinking dialog (Meta+T / Option+T)
send <M-t>
wait "thinking" 3s

# Capture first option (default selected)
# Then navigate to capture each option
send <Down>
wait 100
snapshot "thinking_dialog_disabled_selected"

send <Up>
wait 100
snapshot "thinking_dialog_enabled_selected"

# Close dialog and exit
send <Esc> 100 <C-u> 100 "/exit" 100 <Enter>
wait 500
kill TERM
