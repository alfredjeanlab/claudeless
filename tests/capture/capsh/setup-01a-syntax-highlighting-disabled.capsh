# Capture theme selection dialog with syntax highlighting disabled.
# Config: empty

wait "Dark mode" 10s
send <C-t>
wait "Syntax highlighting disabled" 5s
snapshot "setup_01a_syntax_highlighting_disabled"

send <C-c>
wait 500
kill TERM
