# Capture Bash tool permission dialog.
# Requires API call - Claude must decide to use Bash tool.
# Note: Requires KEYBOARD_TIMEOUT=120 or longer (API response time varies).
# Run with: KEYBOARD_TIMEOUT=120 ./tests/capture/capture.sh --script bash-permission-dialog
# Args: --model haiku

# Handle trust dialog if it appears, then wait for ready state
if wait "trust this folder" 5s
    send <Enter>
    wait "for shortcuts" 10s
else
    wait "for shortcuts" 10s
end

# Ask Claude to run a bash command that requires permission
send "Use the Bash tool to run: touch /tmp/test_file.txt"
wait 100
send <Enter>

# Wait for Bash permission dialog to appear
wait "Bash command" 120s
wait "Do you want to proceed" 10s
snapshot "bash_permission_dialog"

# Cancel and exit cleanly
send <Esc>
wait 500
send <C-u> 100 "/exit" 100 <Enter>
wait 500
kill TERM
