# Capture status bar with "accept edits" permission mode active.
# Args: --model haiku

wait "for shortcuts" 10s

# Send Shift+Tab (BackTab) escape sequence to cycle to "accept edits on" mode
send <Esc> "[Z"
wait "accept edits" 3s
snapshot "permission_accept_edits"

send <C-u> 100 "/exit" 100 <Enter>
wait 500
kill TERM
