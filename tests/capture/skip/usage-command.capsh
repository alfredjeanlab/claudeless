# Capture /usage command showing usage limits panel.
# Args: --model haiku

wait "─\n❯" 10s

send "/usage"
wait 500
send <Enter>
wait "Current session" 5s
snapshot "usage_dialog"

send <Esc>
wait "─\n❯" 3s

send <C-u> 100 "/exit" 100 <Enter>
wait 500
kill TERM
