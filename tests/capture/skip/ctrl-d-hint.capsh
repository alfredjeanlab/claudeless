# Capture the "Press Ctrl-D again to exit" hint.
# Note: Ctrl-D cannot be sent correctly via PTY in raw mode.

wait "for shortcuts" 10s

# Type some text
send "hello world"
wait "hello world" 3s

# Send Ctrl-D
send <C-d>
wait "Ctrl-D" 5s
snapshot "ctrl_d_hint"

send <C-u> 100 "/exit" 100 <Enter>
wait 500
kill TERM
