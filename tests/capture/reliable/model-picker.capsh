# Capture model picker dialog and model selection states.
# Args: (none)

wait "for shortcuts" 10s

# Open model picker (Meta+P / Option+P)
send <M-p>
wait "Select model" 3s
snapshot "model_picker"

# Select Sonnet (second option)
send <Down>
send <Enter>
wait "Sonnet" 3s
snapshot "model_sonnet"

# Open picker again and select Opus (third option)
send <M-p>
wait "Select model" 3s
send <Down><Down>
send <Enter>
wait "Opus" 3s
snapshot "model_opus"

# Open picker again and select Haiku (first option)
send <M-p>
wait "Select model" 3s
send <Up><Up>
send <Enter>
wait "Haiku" 3s
snapshot "model_haiku"

send <C-u> 100 "/exit" 100 <Enter>
wait 500
kill TERM
